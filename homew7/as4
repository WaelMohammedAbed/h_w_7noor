var a = [17, 24, 4, 26, 19, 30, 2, 66, 40];
var min =0;
 {
   
 min=a[1];
    for ( i less than the value of variable a elements length ) {

        if (isOdd(a[i]))
        {
            
              if(min is greater than a[i]))
              min=a[i];
              console.log(a[i]); ;          // this will put the  odds value 
      
       }
    }
       console.log(" min of odds " , min);  // this will put the minimum value 
   
     min=a[1];   
       
        for  i less than the value of variable a elements length) {

        if (not isOdd(a[i])
        {
            
              if(min is greater than a[i])
              min=a[i];
              console.log(a[i]);             //this will put the  evens value 
       
       } 
       
    }
    console.log(" min of evens ",min);      //this will put the minimum value 
    
function isOdd ( parameter a)
if (a divides 2 has a remainder) THEN 
return true
else 
return false
end function